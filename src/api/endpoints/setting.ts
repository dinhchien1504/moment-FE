import API_HOST from "../host";

const SETTING_API = {
    SETTING:  `${API_HOST}/api/account/setting`,
    CHANGE_USERNAME:  `${API_HOST}/api/account/change-username`,
    CHANGE_PASSWORD:  `${API_HOST}/api/account/change-password`,
  };
  
export default SETTING_API;